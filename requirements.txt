# SEEK Backend Dependencies Manifest
# Phase 0 Initial specification

# Web Framework & API Layer
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Asynchronous HTTP & Crawling
httpx>=0.27.0
asyncio>=3.4.3

# Content Parsing & Extraction
beautifulsoup4>=4.12.0
lxml>=5.1.0
trafilatura>=1.8.0

# Information Retrieval & Ranking (Lexical Baseline)
rank-bm25>=0.2.2
nltk>=3.8.1

# Embeddings & Vector Search (Phase 6 Semantic Search, local-only)
# all-MiniLM-L6-v2 is downloaded/cached on first semantic operation; the model
# (and faiss) are loaded lazily so BM25 search works even when these are absent.
sentence-transformers>=2.5.0
faiss-cpu>=1.8.0
numpy>=1.26.0

# Persistence & ORM
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.28
alembic>=1.13.1

# Environment & Utilities
python-dotenv>=1.0.1
pyyaml>=6.0.1

# Testing & Quality Assurance
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
